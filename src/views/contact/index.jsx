import styles from './styles.module.css'

const ContactView = () => (
  <div 
    id="contact-section"
    className={styles.contactContainer}
  >
    <h1>Contact view</h1>
  </div>
)

export default ContactView
